{
  "extends": "./tsconfig.json",
  // Build only the real application entrypoint (and its transitive imports).
  // This prevents incomplete/unwired module stubs from breaking compilation.
  "files": ["src/main.ts"],
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
