# Backend environment example (template)
# Copy to backend/.env (or project root .env) and fill values.

# Core
NODE_ENV=development
PORT=3001
VERSION=local

# Database (required)
DATABASE_URL=postgresql://aos:aos@localhost:5433/aosdb?schema=public

# Redis (optional in test, recommended in runtime)
REDIS_URL=redis://localhost:6379

# Auth (required)
JWT_SECRET=changeme

# Feature flags / runtime switches
# TZ 7: balance validation is ALWAYS ON in production; this flag can only disable it in non-production.
ACCOUNTING_VALIDATE_ON_POST=true
# Legacy flag should be removed from code; keep explicit default for safety
LEGACY_STOCK_ENABLED=false

# OS validation mode (strict|warn|off)
OS_SELF_VALIDATE_MODE=strict

# Optional: test DB used by db-test-setup scripts
TEST_DATABASE_URL=postgresql://aos:aos@localhost:5433/aosdb_e2e_smoke?schema=public

# Integrations (fill as needed)
N8N_PORT=5678


















