/**
 * Shared enums for SCM (Supply Chain Management) module
 * These enums are used across backend, frontend, and shared code
 * to ensure type safety and consistency.
 */

export enum SupplierRole {
  PRODUCER = 'PRODUCER',
  RAW_MATERIAL = 'RAW_MATERIAL',
  PACKAGING = 'PACKAGING',
  PRINTING = 'PRINTING',
  LOGISTICS = 'LOGISTICS',
  OTHER = 'OTHER',
}



























