"use client";

export default function WarehouseLedgerTab(_props: { warehouseId: string }) {
  // Placeholder tab to keep the route buildable.
  // TODO: implement warehouse ledger when backend endpoint is ready.
  return (
    <div className="text-sm text-muted-foreground">
      Ledger is not available yet.
    </div>
  );
}

















