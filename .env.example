# ============================================
# Ly[x]an AOS v0 - Environment Variables
# ============================================
# 
# Скопируйте этот файл в .env и при необходимости
# отредактируйте значения под ваше окружение.
#
# cp .env.example .env
#

# ---------- Common ----------
NODE_ENV=development

# ---------- Backend ----------
PORT=3001
# Для локальной разработки (когда запускаешь backend напрямую на Windows)
DATABASE_URL=postgresql://aos:aos@localhost:5433/aosdb?schema=public
# Для Docker (используется только через BACKEND_DATABASE_URL в docker-compose.yml)
BACKEND_DATABASE_URL=postgresql://aos:aos@postgres:5432/aosdb?schema=public
REDIS_URL=redis://localhost:6379
JWT_SECRET=changeme

# Test database (для e2e тестов)
TEST_DATABASE_URL=postgresql://aos:aos@localhost:5433/aosdb?schema=public

# ---------- Frontend ----------
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# ---------- Redis ----------
REDIS_PORT=6379

# ---------- n8n ----------
N8N_PORT=5678
N8N_HOST=localhost
N8N_PROTOCOL=http
